# Files graphify should not pull into the knowledge graph.
# Parsed with gitignore syntax.

# -------------------------
# Build output
# -------------------------
.gradle/
build/
app/build/
app/release/

# -------------------------
# Vendored, not project code
# -------------------------
# The wrapper scripts are Gradle's, not ours - in the first run they clustered
# into their own community (die(), warn(), gradlew) that says nothing about the app.
gradlew
gradlew.bat
gradle/wrapper/

# -------------------------
# Generated
# -------------------------
# release-please owns this file; it is a rendering of commit titles that are
# already in the graph by way of the workflow config.
CHANGELOG.md

# -------------------------
# Duplicate artwork
# -------------------------
# The launcher icon ships as the same two images at five densities. Extracting
# all ten costs vision passes and yields ten near-identical nodes, so keep only
# xxxhdpi (the largest, and the one worth inspecting).
app/src/main/res/mipmap-mdpi/
app/src/main/res/mipmap-hdpi/
app/src/main/res/mipmap-xhdpi/
app/src/main/res/mipmap-xxhdpi/
