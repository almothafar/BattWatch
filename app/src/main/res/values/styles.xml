<resources>

    <!-- Base application theme - Material Design 3 -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Material3 Color System -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_primaryContainer</item>
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_onSecondary</item>
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>
        <item name="android:colorBackground">@color/md_theme_background</item>
        <item name="colorOnBackground">@color/md_theme_onBackground</item>
        <item name="colorError">@color/md_theme_error</item>
        <item name="colorOnError">@color/md_theme_onError</item>
        <!-- Pin the status bar to the toolbar it sits above. Left to the platform it resolves to colorPrimary, which
             matched the toolbar in light only by coincidence and broke the moment primary lightened for dark.
             Deprecated in API 35 and ignored at targetSdk 35+, so this covers API 26-34 only; from 35 the enforced
             edge-to-edge Toolbar paints that strip itself, out of the same colour and its toolbar_status_bar_padding.
             There is no non-deprecated replacement for the older range, which is why the attribute stays. -->
        <item name="android:statusBarColor">@color/title_bar_background_color</item>
    </style>

    <style name="Widget" />

    <style name="Widget.App.BatteryGauge" parent="Widget">
        <item name="gaugeShowShadow">true</item>
        <item name="gaugeStrokeWidth">30dp</item>
        <item name="gaugeTrackColor">@color/circular_progress_default_background</item>
        <item name="gaugeLevelColor">@color/circular_progress_default_progress</item>
        <item name="gaugeWarningColor">@color/circular_progress_default_progress_warning</item>
        <item name="gaugeAlertColor">@color/circular_progress_default_progress_alert</item>
        <item name="gaugeTitleColor">@color/circular_progress_default_title</item>
        <item name="gaugeTitleTextSize">48dp</item>
        <item name="gaugeStatusColor">@color/circular_progress_default_subtitle</item>
        <item name="gaugeStatusTextSize">20dp</item>
        <item name="gaugeLevelDescription">@string/battery_progress_description</item>
    </style>

    <!-- Shared look for the eight Battery Insights cards, so a surface or elevation tweak lands in one place.
         The background has to be stated: androidx.cardview otherwise picks its own #424242 on a dark theme,
         lighter than every other surface in the app. contentPadding and layout params stay per-card. -->
    <style name="Widget.App.InsightCard" parent="Widget">
        <item name="cardBackgroundColor">?attr/colorSurface</item>
        <item name="cardCornerRadius">8dp</item>
        <item name="cardElevation">4dp</item>
    </style>

    <!-- Toolbar style for Material3 -->
    <style name="ToolBarStyle" parent="ThemeOverlay.Material3.Toolbar.Surface">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
        <!-- Tint control icons (the overflow three-dots) white to match the white gear -->
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <!-- Toolbar overflow popup; see the night variant for why it is its own style. -->
    <style name="ToolBarStyle.Popup" parent="ThemeOverlay.Material3.Light" />

    <!-- Slightly smaller toolbar title so the long app name doesn't dominate the bar.
         Color stays white via the Toolbar's app:titleTextColor. -->
    <style name="ToolBarStyle.Title" parent="TextAppearance.Material3.TitleLarge">
        <item name="android:textSize">18sp</item>
    </style>

    <!-- Settings theme without ActionBar (we use Toolbar instead) -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Ensure system bars are NOT translucent to prevent overlap -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <!-- Details-table cells (labels + values, its only users). TextAppearance.Medium's 18sp read a
         touch large on-device (#174); 16sp — tune here if it needs another nudge. -->
    <style name="DefaultTextStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">16sp</item>
    </style>

    <!-- SeekBar Preference style with better touch targets -->
    <style name="PreferenceSeekBar" parent="@style/Preference.SeekBarPreference.Material">
        <item name="android:minHeight">64dp</item>
        <item name="seekBarIncrement">1</item>
    </style>

    <!-- Shared look for the critical/warning RangeSlider (Home + Settings) so size/color tweaks
         live in one place. Material 1.14's slider defaults to tall bar thumbs and draws a tick dot
         per step (40 dots over 10-50), which looks noisy; here we hide the ticks, use a compact
         round thumb snug to the track, drop the floating bubble (the captions show the values),
         and keep a comfortable touch target. -->
    <style name="Widget.App.RangeSlider" parent="Widget.Material3.Slider">
        <item name="labelBehavior">gone</item>
        <item name="tickVisible">false</item>
        <item name="thumbWidth">16dp</item>
        <item name="thumbHeight">16dp</item>
        <item name="thumbTrackGapSize">0dp</item>
        <item name="trackHeight">4dp</item>
        <item name="trackStopIndicatorSize">0dp</item>
        <item name="minTouchTargetSize">40dp</item>
    </style>
</resources>
