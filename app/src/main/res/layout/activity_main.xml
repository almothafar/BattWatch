<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.almothafar.simplebatterynotifier.ui.MainActivity"
    android:id="@+id/containerLayout">

    <!-- Modern Toolbar -->
    <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="?attr/actionBarSize"
            android:paddingTop="@dimen/toolbar_status_bar_padding"
            android:background="@color/title_bar_background_color"
            android:elevation="4dp"
            android:theme="@style/ToolBarStyle"
            app:popupTheme="@style/ToolBarStyle.Popup"
            app:title="@string/app_name"
            app:titleTextAppearance="@style/ToolBarStyle.Title"
            app:titleTextColor="@android:color/white"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/top_background_color"
        android:id="@+id/percentageLayout">

        <com.almothafar.simplebatterynotifier.ui.widget.HorseshoeProgressBar
            android:id="@+id/batteryPercentage"
            style="@style/Widget.App.BatteryGauge"
            android:layout_width="wrap_content"
            android:layout_height="160dp"
            android:layout_gravity="center"
            tools:gaugeTitle="100%"
            tools:gaugeStatusText="Charging"
            android:layout_marginTop="-2dp"
            android:layout_marginBottom="-10dp" />

        <!-- Honest info affordance for the smoothed/estimated live percentage (#217). Overlaid in the
             top corner of the gauge area; tinted white for the coloured header. 48dp to meet the minimum
             touch target, with the padding carrying the glyph back to 24dp so it looks as it always has. -->
        <ImageButton
            android:id="@+id/gaugeInfoButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="6dp"
            android:padding="12dp"
            android:src="@drawable/ic_info"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/gauge_info_content_description"
            app:tint="@android:color/white" />
        <!-- Theme toggle, mirroring the info button in the opposite corner (#333). Flips light/dark on the spot; the glyph
             names where a tap goes rather than where it already is, and MainActivity offers System back when a tap leaves it.
             The glyph is preview-only here: MainActivity sets the real one, and a src naming either state would leave the test
             for that state passing with the code that sets it deleted. -->
        <ImageButton
            android:id="@+id/gaugeThemeButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="top|start"
            android:layout_marginTop="6dp"
            android:layout_marginStart="6dp"
            android:padding="12dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/gauge_theme_switch_to_dark"
            tools:src="@drawable/ic_dark_mode"
            app:tint="@android:color/white" />
    </FrameLayout>

    <!-- In-fly critical/warning threshold adjuster, placed directly under the gauge it controls.
         Mirrors the Settings range slider and writes the same preference keys, so changes here
         update the gauge immediately. -->
    <LinearLayout
        android:id="@+id/thresholdSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="?android:attr/colorBackground"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="4dp"
        android:paddingBottom="4dp">

        <com.google.android.material.slider.RangeSlider
            android:id="@+id/thresholdSlider"
            style="@style/Widget.App.RangeSlider"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stepSize="1"
            android:valueFrom="10"
            android:valueTo="50" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="-6dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/thresholdCriticalCaption"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="start"
                android:textColor="@color/battery_rate_high"
                android:textSize="12sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/thresholdWarningCaption"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="end"
                android:textColor="@color/battery_rate_warn"
                android:textSize="12sp"
                android:textStyle="bold" />

        </LinearLayout>

    </LinearLayout>

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/detailsFragmentLayout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:name="com.almothafar.simplebatterynotifier.ui.fragment.BatteryDetailsFragment"
        tools:layout="@layout/fragment_battery_details" />

    <!-- Battery Health Section -->
    <LinearLayout
        android:id="@+id/homeBottomSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="@dimen/content_navigation_bar_padding"
        android:background="?android:attr/colorBackground">

        <!-- Battery Insights Button - Material3 -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/batteryInsightsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/view_battery_insights"
            android:textColor="@android:color/white"
            app:backgroundTint="@color/top_background_color"
            android:layout_marginBottom="8dp"
            style="@style/Widget.Material3.Button" />

        <!-- Developer Signature Fragment -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/signatureFragmentLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:name="com.almothafar.simplebatterynotifier.ui.fragment.SignatureFragment"
            tools:layout="@layout/fragment_signature" />

    </LinearLayout>

</LinearLayout>
